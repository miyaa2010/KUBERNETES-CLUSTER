apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: production
data:
  database_url: "mysql://mysql-svc:3306/mydb"
  log_level: "info"
  app.conf: |
    server.port=8080
    timeout=30