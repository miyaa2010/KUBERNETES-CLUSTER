#dùng cho StatefulSet, trả về trực tiếp IP của Pod
apiVersion: v1
kind: Service
metadata:
  name: mysql-headless
spec:
  clusterIP: None                  # Quan trọng: không cấp ClusterIP
  selector:
    app: mysql
  ports:
    - port: 3306
      name: mysql
